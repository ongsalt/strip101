[package]
name = "strip101"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
image = "0.25.10"
usvg = "0.47.0"
vello_cpu = { version = "0.0.9", features = ["multithreading"]}
